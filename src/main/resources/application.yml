spring:
  application:
    name: dram-bridge-model
  profiles:
    active: local

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info

app:
  market-data:
    freshness:
      max-age-hours: 18
      required-prices: BATS:DRAM,NASDAQ:MU,NASDAQ:SNDK,NASDAQ:WDC,NASDAQ:STX
