spring:
  datasource:
    url: jdbc:h2:mem:dram_bridge_test_${random.int};MODE=MySQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

app:
  seed:
    enabled: false
